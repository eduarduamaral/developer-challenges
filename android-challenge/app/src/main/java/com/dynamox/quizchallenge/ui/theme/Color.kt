package com.dynamox.quizchallenge.ui.theme

import androidx.compose.ui.graphics.Color

val DynamoxBlue = Color(0xFF3949AB)
val DynamoxBlueDark = Color(0xFF283593)

// Semantic success/error colors for quiz feedback, kept separate from the brand's
// primary/secondary palette above: "correct" should always read as green and
// "incorrect" as red, regardless of what the app's primary brand color happens to be.
val SuccessGreen = Color(0xFF2E7D32)
val SuccessGreenContainer = Color(0xFFC8E6C9)
